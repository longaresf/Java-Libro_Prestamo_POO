# Java-Libro_Prestamo_POO

## Description
This repository contains a Java Desktop Application named "Libro_Prestamo_POO". The application implements basic functionalities related to book lending using Object-Oriented Programming (POO) principles.

## Tech Stack
- **Java**: Primary programming language used for developing the desktop application.
- **JDK**: Java Development Kit is assumed to be installed as part of the development environment.
  
## Usage

### Prerequisites
Before running this application, ensure that you have the following prerequisites:
- JDK 1.8 or higher installed on your system.

### Running the Application
To run the "Libro_Prestamo_POO" application, follow these steps:

1. **Clone the Repository**: Clone this repository to your local machine using Git.
   ```bash
   git clone https://github.com/your-username/Java-Libro_Prestamo_POO.git

2. **Navigate to the Directory**:
   ```bash
   cd Java-Libro_Prestamo_POO

3. **Build the Application**: Use Maven or Gradle to compile and build the project.
   - For Maven users, run `mvn clean install`.
   - For Gradle users, run `./gradlew build`.

4. **Run the Application**:
   The application will be compiled into a JAR file in the target directory. To run it, execute the following command:
   ```bash
   java -jar target/Libro_Prestamo_POO.jar

### Directory Structure

The repository structure is as follows:
Java-Libro_Prestamo_POO/
├── src/main/java/com/example/libroprestamo/  # Contains Java source files for the application.
│   └── LibroPrestamo.java
├── build.gradle  # Gradle configuration file for building the project.
└── README.md  # This file contains information about the repository.

### License

This software is licensed under the [Apache License, Version 2.0](LICENSE).

---

For any further assistance or questions regarding this application, please contact the maintainers at [support@example.com].
Note: The provided files do not contain `gradle`, `build.xml`, or `src/main/java` directories, nor do they include `manage.py`. Therefore, based on the given file structure and language indicators, it is assumed to be a Java Desktop Application.